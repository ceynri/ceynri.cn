# .agents 使用指南

ceynri.cn 的 AI 资产目录。项目级 AI 指令统一由根目录 `AGENTS.md` 承载（CodeBuddy / Cursor / Claude Code 均自动加载）。

本目录当前仅用于存放未来可能新增的**条件触发型**规则（`alwaysApply: false`，按 glob 或场景触发），暂为空。

`.codebuddy` 为整体软链接到此目录，结构保持一致。
